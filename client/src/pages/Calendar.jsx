import { CalendarDays } from "lucide-react";
import { useTasks } from "../context/TasksContext";
export default function Calendar(){const {tasks}=useTasks();const days=[...Array(14)].map((_,i)=>{const d=new Date();d.setHours(0,0,0,0);d.setDate(d.getDate()+i);return d});return <div className="workspace-page"><header className="page-header"><div><div className="breadcrumb">Manage / Calendar</div><h1>Calendar</h1><p>Task due dates visualized from the existing task API.</p></div></header><div className="calendar-grid">{days.map(day=>{const items=tasks.filter(t=>{const d=new Date(t.dueDate);return d.toDateString()===day.toDateString()});return <div className="calendar-day" key={day.toISOString()}><div className="calendar-day-head"><strong>{day.toLocaleDateString(undefined,{weekday:'short'})}</strong><span>{day.getDate()}</span></div>{items.map(t=><div className={`calendar-task ${t.status==='Completed'?'done':''}`} key={t.id}>{t.title}</div>)}{!items.length&&<span className="calendar-empty">No tasks</span>}</div>})}</div><div className="board-note"><CalendarDays size={14}/> This view uses existing task due dates only; no calendar backend or data model was introduced.</div></div>}
